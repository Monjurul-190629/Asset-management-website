

const SectionTitle = ({heading}) => {
    return (
        <div className="text-center mx-auto">
            <p className="uppercase text-3xl mb-5">{heading}</p>
        </div>
    );
};

export default SectionTitle;